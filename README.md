# @std/bitflags — Bit Flag Types for Zeta

Auto-converted from [bitflags](https://crates.io/crates/bitflags) v2.11.1 via Dark Factory.

```zeta
use @std/bitflags::bitflags;

bitflags! { struct Flags: u32 {
    const A = 1;
    const B = 2;
    const C = 4;
}}
```

## Stats: 13 source files, ~1,451 lines, 0 unsupported
